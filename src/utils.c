# include "cub3d.h"

/*
void	calc_ray(int x, t_ray *ray, t_vector *player, t_camera *camera)
{
	double	camera_x;

	camera_x = 2 * x / (double) camera->width - 1;
	ray->dir.x = player->dir.x + player->plane.x * camera_x;
	ray->dir.y = player->dir.y + player->plane.y * camera_x;
}

// TODO
t_vector	ft_normalize_vector(t_vector v1, t_vector v2)
{
}

// TODO
t_vector	ft_scale_vector(t_vector v1, t_vector v2)
{
}

// TODO
t_vector	ft_rotate_vector(t_vector v1, t_vector v2)
{
}

// TODO
t_vector	ft_add_vector(t_vector v1, t_vector v2)
{
}
*/
